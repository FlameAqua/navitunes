<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Downloaded update APKs live in cacheDir/updates and are shared, read-only,
         with the system package installer via FileProvider. -->
    <cache-path name="updates" path="updates/" />
</paths>
